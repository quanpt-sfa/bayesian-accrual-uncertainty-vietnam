# -----------------------------------------------------------------------------
# Script: 13_grouped_kfold_firm.R
# Purpose: Exact grouped K-fold by firm.
#
# TEMPORARY SAFE WRAPPER:
#   This file was intentionally replaced with a safe blocker after a failed large
#   contents-API update attempt. Do not run production from this placeholder.
#   Restore the full reviewer-final implementation before running script 13.
# -----------------------------------------------------------------------------

stop(
  "[BLOCKER] scripts/13_grouped_kfold_firm.R is temporarily locked after a failed repo update. ",
  "Restore the full reviewer-final implementation before running exact grouped K-fold."
)
