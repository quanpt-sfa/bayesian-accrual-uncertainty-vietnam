# brms worker refactor audit

Scope: `run.R`, `scripts/**/*.R`, and `tests/**/*.R` were scanned for `brms::brm(`, `brm(`, `sample_prior = "only"`, `saveRDS(fit`, `posterior_predict(`, and `posterior_epred(`.

| file | brms_call_type | task_unit | current_parallelism | current_shared_writes | worker_refactor_decision | recommended_pattern | risk_level | notes |
|---|---|---|---|---|---|---|---|---|
| `scripts/ma06_prior_predictive_checks.R` | `brms::brm` via worker `do.call`, `sample_prior = "only"`, `posterior_predict` | representative model configuration | ma07-style worker pool when `ACCRUAL_ENABLE_MODEL_PARALLEL=TRUE`; sequential by default | parent writes prior predictive summary, extreme-rate table, gate status, notes, and figures | `workerize_now` | keep one script; worker fits one prior-predictive task and returns summary/extreme/simulation; parent writes shared outputs | medium | implemented in this patch; seed remains `baseline_prior_predictive_fit` with original task index offset |
| `scripts/ma07a_fit_brms_named_models.R` | `brms::brm`, `saveRDS(fit)` | baseline/remediation model row | ma07-style model worker pool | parent writes ma07a task manifest/status only; shared diagnostics are collected by ma07b | `already_workerized` | use common `accrual_run_task_pool()` and task-level fit/meta/log artifacts | low | generalized to common helper in this patch |
| `scripts/ma07b_collect_brms_fit_outputs.R` | `posterior_epred`, `posterior_predict` only | existing fit artifact | none; collector stage | writes downstream diagnostics, coefficient summaries, draw artifacts, audit tables | `no_action_non_brms` | keep collector-only; do not fit | low | no `brm()` call |
| `scripts/ma09_loo_stacking.R` | `brm`, `saveRDS(fit_sp)` for save-pars refits | selected model requiring LOO-compatible refit | sequential rstan chain-level cores | LOO objects, stacking weights, cache files, and shared tables | `split_fit_collect_now` | future split: `ma09a_refit_savepars` and `ma09b_compute_loo_stacking`; workerize only independent refits | high | deferred from this patch to avoid changing PSIS/LOO collector semantics; PSIS remains secondary |
| `scripts/ma12_grouped_kfold_firm.R` | `brm`, `saveRDS(fit)`, `posterior_epred`, `posterior_predict` | grouped fold x model x target-space task | sequential task loop with centralized rstan cores | fold diagnostics, score tables, run manifests, weights, pins | `split_fit_collect_now` | future split: `ma12a_grouped_kfold_plan`, `ma12b_grouped_kfold_fit`, `ma12c_grouped_kfold_score_collect` | high | deferred because exact K-fold cache invalidation, pins, weights, and DA scores must remain parent/collector-only |
| `scripts/ma13_row_level_exact_kfold.R` | `brm`, `saveRDS(fit)` | row fold x model x target-space task | sequential task loop with centralized rstan cores | row K-fold diagnostics, score tables, run manifests, pins | `split_fit_collect_now` | future split: `ma13a_row_kfold_plan`, `ma13b_row_kfold_fit`, `ma13c_row_kfold_score_collect` | high | deferred because row-level validation-target semantics and cache provenance are primary-method sensitive |
| `scripts/sensitivity/se01_prior_predictive.R` | `brms::brm` via worker `do.call`, `sample_prior = "only"`, `saveRDS(fit)`, `posterior_predict` | sensitivity scenario x eligible formula | ma07-style worker pool when `ACCRUAL_ENABLE_MODEL_PARALLEL=TRUE`; sequential by default | parent writes scenario prior predictive tables, aggregate summary, gate table, notes, and manifests | `workerize_now` | keep one script; worker fits one scenario-formula task and writes only task-level fit/log artifacts; parent writes shared outputs | medium | implemented in this patch; seed preserves `sensitivity_prior_predictive_<scenario>_<target_space>_<Model_ID>` with original scenario/formula offset |
| `scripts/sensitivity/se02_refit_prior_scenarios.R` | `brms::brm`, `saveRDS(fit)`, `posterior_epred`, `posterior_predict` | sensitivity scenario x model/formula | sequential; centralized sensitivity sampler | diagnostics, summaries, stacking inputs, manifests, status tables | `split_fit_collect_now` | future split: `se02a_plan_prior_scenario_refits`, `se02b_fit_prior_scenario_models`, `se02c_collect_prior_scenario_outputs` | high | deferred because it mixes fit and collect responsibilities across sensitivity reporting |
| `scripts/simulation/si03_brms_leakage_confirmation.R` | `brms::brm` | simulation replicate/model | local simulation runtime only | simulation outputs only | `defer_simulation_only` | optional future simulation worker profile, isolated from production tables | medium | not production baseline/sensitivity output |
| `scripts/simulation/si04_brms_parameter_recovery.R` | `brms::brm` | simulation replicate/model | local simulation runtime only | simulation outputs only | `defer_simulation_only` | optional future simulation worker profile, isolated from production tables | medium | not production baseline/sensitivity output |
| `scripts/diagnostics/di08_mcmc_sampler_calibration.R` | `brms::brm`, `saveRDS(fit)` | diagnostic calibration scenario | diagnostic-only loop | diagnostic calibration outputs only | `defer_diagnostic_only` | optional future diagnostic worker profile; never writes production tables from workers | medium | isolated calibration, not part of main production fit collection |

Decision note: `ma06`, `se01`, and `ma07a` are the production scripts changed in this patch. Other production fit/collect scripts are deliberately documented as split candidates rather than refactored in the same pass, because their collectors own primary K-fold pins, stacking weights, or sensitivity reporting outputs.
